---
layout: base.njk
---

<article class="post-article">
  <header class="post-header">
    <h1>{{ post.title }}</h1>
    <time datetime="{{ post.published }}">{{ post.published | date("dd/MM/yyyy") }}</time>
  </header>

  {{ post.content | safe }}
</article>
