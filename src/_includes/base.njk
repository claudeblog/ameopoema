<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <title>{{ post.title or title or site.title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/gate.css' | url }}">
  </head>

  <body class="locked">
    <header class="site-header">
      {% include "partials/site-nav.njk" %}
    </header>

    {% include "partials/gate.njk" %}

    <main class="site-main">
      {{ content | safe }}
    </main>

    {% include "partials/site-footer.njk" %}

    <script src="{{ '/js/gate.js' | url }}" defer></script>
  </body>
</html>
